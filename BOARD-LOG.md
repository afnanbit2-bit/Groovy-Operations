# The Board — session 2 run log

Continuous run: P0, then P1, then P2, per the session-2 brief and Ammar's
decisions of Sat 26 Sep 2026. One commit per item, pushed to
`claude/session-brief-spec-setup-g0dofb` immediately; `CACHE_VERSION` bumped
past `origin/main` on every commit.

**Nothing here reaches the live site until the branch's PR is merged into
`main`** — Netlify deploys `main` only.

Every commit below passed, before it was pushed:
`node --check` on every script, `node tests/run.js`, and
`node tests/smoke-board.js` in real Chromium (plus the other smoke tests
where the commit touched what they measure).

Lines marked **→ NEEDS YOU** are actions for a human.

---

## Decisions taken mid-run (the conservative option, per the brief)

- The run brief's P2 list arrived truncated after "08:00 PKT reminder as a
  scheduled Netlify function (due_today +". P2 is built from the approved
  plan: the reminder (due_today + overdue, deduped per item per day), the
  admin screen (markers + pin), the inbox listener's source filter, cached
  lookups, and the acceptance-script prep.

---

## Commits

| # | Commit | What | CACHE_VERSION | Tests |
|---|---|---|---|---|
| P0.1 | `c06ad14` | **Calendar freeze.** `window.tbCalFilter=` (dropdown handler) replaced the pure `function tbCalFilter` — one binding in a browser — so the calendar recursed into its own repaint (~17s lock, then stack overflow). Handler renamed `tbCalSetFilter` (definition + 4 dropdowns; the by-person call at the old line 3220 already calls the pure filter and resolves correctly now). The handler refuses unknown keys. Stored prefs are REBUILT from the six known keys on load; anything over 4 KB or unreadable is removed. New repo-wide invariant: no `window.X=` may replace a same-named top-level function (self-alias and capture-and-call wraps allowed). New `tests/smoke-board.js` + CI step: real Chromium, every Board page, every calendar toolbar control, three modes (Ammar, Saim, Ammar with the 1.36 MB junk filter); freeze detected structurally by repaint nesting, not the clock (virtual time freezes `performance.now()`). **Proven against the shipped file: all three modes fail (stuck page); all pass on the fix.** | v166 | run.js 5,487/5,487 · smoke-board 3/3 · smoke-browser 7/7 · smoke-startapp 4/4 · smoke-layout Board fragments 54/54 |
| P0.2 | `a96cddb` | **People.** Nothing on the Board loaded the profile DIRECTORY (only your own row, from profileBootstrap), so every other person rendered as "someone", Team today listed one person, and the assign chips, @mentions, handover, the person filter and bell addressing (`_tbNotify` needs a handle) all knew nobody but you. New `tbPeople()` resolves the five Board users from profile rows (or the session, for yourself); `loadTbData` now loads the directory (`loadProfiles`) with the Board data and names `user_profiles` in the warning strip if refused. Team today lists **all five from day one** ("not set up yet" where no profile row exists) — so the one-sentence empty state now keys off YOUR column only (two old tests encoding "no team card on an empty board" rewritten to the new rule). The drawer lists all five, a not-set-up person disabled and saying why. `@afnan` in quick add is an assignee; a Board person with no row yet is **never title text** — taken out, named in the preview and a toast. | v167 | run.js 5,511/5,511 · smoke-board 3/3 (88 checks each: five Team rows, no "someone", @afnan → assignee) · smoke-browser 7/7 · smoke-startapp 4/4 · layout Board 54/54 |
| P0.3 | `14ad9f3` | **Live items.** The board was read ONCE per session (again only after a refused write or a Retry), so a date moved by one person never reached another's open screen. Now the same four queries `loadTbData` reads (shared items, my items, shared lists, my lists) plus the markers doc are LISTENED to. The first paint stays on `getDocs` (the harness's `onSnapshot` never fires and every suite depends on it); listeners start after it with per-query maps seeded from that read, and each snapshot replaces only its own map, so a listener that has not delivered never drops what the screen showed. **A remote update never repaints mid-gesture**: data is taken at once, the repaint waits while a field has focus or a pill is being dragged, and lands on focusout/pointerup (700 ms retry). Off the Board: memory updates, nothing paints. No listener in an old cached shell → the board stays static, as before. Sign-out reloads the page, so no listener outlives a session. **Cost:** each listener's first snapshot re-reads its query once per session (~50 docs at today's size). | v168 | run.js 5,531/5,531 · smoke-board 3/3 (94 checks each: a colleague's write lands without reload, waits while typing, lands on blur, a remote delete goes) · smoke-browser 7/7 · smoke-startapp 4/4 |
| merge | `953e1e7` | Merged `origin/main` (Afnan's Store Accounts edit + warehouse-sales review fixes, `v166`). Conflicts only in `sw.js` (`CACHE_VERSION` v168 vs v166) and `index.html` (the `main.css` `?v=` tag); resolved **past both** — the merge is new bytes. | v169 | run.js 5,722/5,722 (includes main's new suites) · smoke-board 3/3 · smoke-browser 7/7 · smoke-startapp 4/4 · **full** smoke-layout 304/304 |
| P0.4 | `9e3b521` | **Seed: Run seed button + script fix.** One seed body, `scripts/board-seed-plan.js` (pure `buildSeedPlan` + a `runSeed` that takes firebase-admin handles), shared by a new **`netlify/functions/board-seed.js`** (POST, the caller's ID token verified server-side, Board owners only by email — mirrored against `isBoardOwner()` and `BOARD_OWNERS` by test — `dryRun` supported, returns counts) and the CLI `scripts/seed-board.js`, which now **writes by default** with `--dry-run` to preview. A **missing login is skipped and named**, never fatal (the old script stopped outright); an item whose only assignee has no login is skipped and named. **Idempotent**: deterministic ids, KEEP_FIELDS never re-written, and a re-run no longer rewrites `createdAt`/`updatedAt`/`lastActivityAt`; list members are a union (a re-run adds, never removes). **It also writes a `{uid, username}` profile row (merge) for each Board person**, so the seed fixes Team today on its own. The client: an owner-only **Board settings** button in the rail (desktop, ≥1024px — below that the rail is a sideways strip and smoke-layout measured the button past its end; P1's rail rebuild places it on phones) → overlay with Preview / Run seed; the result is shown as text (hydrated), a refusal or a not-yet-deployed function is said out loud; after a real run the directory and the board are re-read. Moved rows verified byte-identical to the old script's. Bundled with esbuild (Netlify's bundler) in the scratchpad to prove the `../../scripts/` require resolves. | v170 | run.js 5,809/5,809 (new `board-seed` suite: 71) · smoke-board 3/3 (Ammar 100, Saim 95: owner-only button, a missing function says so) · smoke-browser 7/7 · smoke-startapp 4/4 · layout Board fragments 54/54 (first run failed the rail at 420px — fixed before push) |
| P0.1-fix | `4a2da49` | **Adversarial review of `c06ad14` (P0.1) — 2 confirmed, both in my tests, both fixed.** (1) *major:* `smoke-board.js` read the real clock, and the seed's dates are fixed, so from **1 Dec 2026** the month grid held no seeded item and the new CI step would have gone red for both tracks with no code change (the reviewer reproduced it with a Date shim); the pill/drawer checks also skipped silently behind `if(pill)`. The page clock is now **pinned to 26 Sep 2026 09:00 PKT** (only `new Date()`/`Date.now()` move; timers are real), a check asserts the pin, and a missing pill/row/list is a FAIL rather than a shorter run. Verified: moving the pin to 10 Dec fails the clock check in all three modes. (2) *minor:* the unit assertion "tbCalFilter is still the pure filter" could not fail with the collision restored (the harness `window` is not the vm global) — renamed to what it proves and pointed at the real guards (the invariant, smoke-board). No app code changed. | v171 | run.js 5,809/5,809 · smoke-board 3/3 (Saim 99, Ammar 104) |
| P0.5 | `05431c2` | **Quick add composer, and Needs a date.** The quick-add opens on focus into a chip row: **Date** (Today / Tomorrow / Next Mon — the Monday *after* today — / a date field / clear), **Assign** (the five: "you" always on, a not-set-up person dashed and disabled), **List** and **Lane**. Enter creates, clears, stays open, caret back. **No date means undated — never today** (it used to default to today on the Dashboard). A date typed in the title fills the Date chip live ("from the title"); a picked date, or "no date" picked on the chip, outranks it. `tbComposerPlan` is the one pure decision the preview and the create both read. The composer's state lives in memory (`_tbQa`), not the DOM, so a live repaint redraws it intact and the caret is restored; chip clicks keep the caret in the title (`pointerdown` preventDefault) and repaint only the chip row. Escape clears a half-typed title first, closes on the second press; a click outside closes only an EMPTY composer. Used on the Dashboard and in a list (whose list is the default). **Decision 5:** Needs a date = undated items I own OR am assigned to; Assigned to me no longer repeats the undated ones. The namespace test's allow-list gained `on` (the file's selected-state modifier, previously only concatenated). New `smoke-layout` fragment: the composer open (proven by breaking the chip ink — fails at 1280/1900 both themes). | v172 | run.js 5,842/5,842 · smoke-board 3/3 (Saim 107, Ammar 112: focus opens it, tomorrow + Daniyal chips, Enter creates with both, caret back, a bare title is undated) · smoke-browser 7/7 · smoke-startapp 4/4 · layout Board 60/60 |
| P0.6 | `fdaf96f` | **Naming.** `TB_NAME` is **"The Board"** (it drives the sidebar entry, the phone More sheets and the access message). Title Case on the rail (Dashboard · Calendar · Lists · Inbox), every card title (Overdue · Due Today · My Day · Assigned to Me · Needs a Date · Next 7 Days · Assigned by Me · Deadlines · Inbox · Team Today · Activity · My Lists · Open · Done), the Lists sections (Team · Private · + New), Board Settings, and "Nothing on The Board today". Bell rows are titled `TB_NAME`. In `js/shared.js`: the bug-tracker page names ("The Board — Calendar" …), the designer's phone tab ("The Board"), the nav fallback. Body copy stays sentence case. 11 assertions that matched the old lowercase strings updated; new assertions pin the constant, the rail, every card title, and the three `js/shared.js` strings; smoke-board checks the sidebar and rail text in Chromium. | v173 | run.js 5,849/5,849 · smoke-board 3/3 · smoke-browser 7/7 · smoke-startapp 4/4 · smoke-phone 30/30 · full smoke-layout 310/310 |
| P0.7 | `b43a3db` | **The Board is home.** All five Board users (Ammar, Afnan, Mustafa, Daniyal, Saim) now **land on The Board** after sign-in; each role's own data still loads exactly as before, and every other account lands where it always did (asserted for Arfat — a manager not on The Board — and a worker). The landing lives in `js/auth.js` `startApp` (two lines: a `boardHome` for Board users), not `js/shared.js` — that is where the page after login is chosen. **The phone More sheet already listed The Board first** for Ammar, Afnan and Mustafa (`openMoreSheet`) and Daniyal (`openMktMoreSheet`); Saim's phone bar IS the Board — now proven by driving the sheets, not assumed. One assertion in **`tests/marketing.test.js`** (Ammar's track) grepped for the literal `showPage('mkt-creators')`; updated to the new line, keeping its meaning (a lead NOT on The Board still lands on Creators). | v174 | run.js 5,861/5,861 · smoke-board 3/3 (+ "a Board user lands on The Board") · smoke-browser 7/7 · smoke-startapp 4/4 · smoke-phone 30/30 |
| P1.1 | `302879a` | **Tokens, the Lucide sprite and flatpickr.** Two new vendored files, both fetched from `registry.npmjs.org` with the tarball's sha512 checked against the registry's `dist.integrity` (recorded in `assets/vendor/README.md`), licences beside them, version in the filename, **precached** in `sw.js`: **`lucide-sprite-1.48.0.svg`** (47 icons as `<symbol id="lucide-*">`, built by the new `scripts/build-lucide-sprite.js` from the `lucide-static` package — re-running it reproduces the file byte for byte) and **flatpickr 4.6.13** (min.js + min.css; its dark theme is NOT vendored — the calendar is restyled onto the tokens instead, so one set of colours serves both themes). **The `tb-tokens` block** at the head of the Board's CSS: Radix slate (neutral) and blue (accent) scales 1–12 in light and dark, Open Props shadow-3 and ease-out-3 values COPIED (not vendored), a 12–28 px type scale, a 4–32 spacing scale, three radii, a mono stack. **Measured contrast drove two token choices:** white on blue-9 is 3.26:1, so the accent FILL carries icons only (`--tb-accent` is blue-11 in light, 7.4:1 under white); blue-11 on blue-3 is 4.25:1, so ink on an accent-tinted chip is blue-12. `_tbIcon(name,size)` renders `<svg><use href="sprite#lucide-name">` and returns nothing for a name not on `TB_ICONS` (so a typo is an empty span, never a broken `<use>`). The external `<use>` was verified to render in Chromium (15×15.8 bbox, follows `currentColor`). Nothing on screen uses the tokens yet — P1.2 onward does. New invariants: every vendored file is precached, licensed and referenced by `index.html`; the sprite carries every `TB_ICONS` name. | v175 | run.js 5,890/5,890 · smoke-board 3/3 · smoke-browser 8/8 (new: flatpickr opens and returns a date) · smoke-startapp 4/4 · cache-version ok · layout Board 60/60 |
| P1.2 | `f256c83` | **The rail, rebuilt (To Do pattern).** Desktop ≥1024: a **240px rail** beside the content — Lucide icon + label on every entry, **count pills** (Dashboard = overdue + due today for you, red ink only when one is overdue; Calendar = your open dated items this Mon–Sun week; Inbox = the live unread count, still painted by the listener into the same `#tb-rail-n`), a **Lists group** (every live list with a team/private icon and its open count, the open list marked current; the **Lists header always opens the overview**), and a foot with **"+ New list"** and, for owners, **Settings**. The active entry is a soft accent row with a 3px accent bar; the content stops at 960px (the calendar keeps the full width). Tablet 640–1023: the same rail, **56px, icon-only**, counts as corner badges, lists behind the Lists entry. Phone <640: a **strip across the top, icon over label**, Settings included (the P0.4 note that it had no place on phones is closed). **The search box and the ? moved out of the rail into a header row** that opens every screen (its name, the search, the shortcut list). New pure `tbRailCounts`. **Decision (conservative): "+ New list" in the rail makes a PRIVATE list** — a team list is made from the Lists screen's Team "+ New", so nothing becomes visible to four people by accident. The third pane (detail) is P1.4. **Found on the way:** the P1.1 icon invariant sliced 8 characters off `_tbIcon('` (it is 9), so it passed with any name — fixed, and proven by an unlisted icon failing it. | v176 | run.js 5,914/5,914 · smoke-board 3/3 (Saim 119, Ammar 124: a rail icon draws from the sprite, a rail list opens its list and is current, the Lists header opens the overview, search lives in the header) · smoke-browser 8/8 · smoke-startapp 4/4 · smoke-phone 30/30 · layout Board 72/72 (the shell fragment now measures the LOADED rail at 1900/1280/**800**/420; proven by breaking the pill ink — fails all 8) |
| P0.6-fix | `2b34547` | **Card titles really are Title Case now.** P0.6 changed the markup ("Overdue", "Due Today" …) but `.tb-cardh` carried `text-transform:lowercase`, so every Dashboard card title was still **lowercase on screen**. The P0.6 check read `textContent`, which does not see CSS. Found while looking at the P1.2 screenshots. The rule no longer transforms case; the Dashboard's day heading reads "Saturday 26 September" (it was forced lowercase in JS); the list screen's back button and badges read "Lists", "Team"/"Private", "Admin". **Two new guards, both proven by restoring the lowercase rule:** an invariant that no `.tb-` CSS rule transforms case (names `.tb-cardh`), and smoke-board reading every card title with `innerText` (what is on screen, after CSS) — fails "overdue" in all three modes. Also fixes the log's own table spacing. | v177 | run.js 5,915/5,915 · smoke-board 3/3 (121/126/126) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 72/72 |
| P1.3 | `2ab0a8f` | **The row (To Do pattern).** Every item row, everywhere: a **round checkbox** (the check shows in the accent on hover; done fills the circle), the title on up to two lines, a **meta line under it** — colour dot, kind ("Gate"), date with a calendar icon ("Today", "Oct 2"; red only when overdue), steps, comments, lock, **the list** (off that list's own screen), the other assignees — and a **star on the right that puts the item in My Day** (the existing `tbAddToMyDay`; acts without opening the item; "Added to My Day" toast). 44px minimum, 48 on a phone; the meta line wraps rather than overflowing. **A row with nothing to say has no meta line** (a lone dot on its own line was a wasted line, seen on the screenshots). **Completed group per list**: a list's done items sit in their own group at its foot, most recently completed first, **open by default**, folded by its header and remembered per list for the session (memory, not localStorage). **Decision:** a starred row is the accent ink on a soft accent tile, not a filled star — the sprite sets `fill="none"` on each symbol, which CSS cannot reach through `<use>`; a filled glyph would need a new sprite file (vendored files are never edited in place). **Found on the way:** P1.2 made `.tb-wrap` a grid, so two layout fragments that wrapped content in `.tb-wrap` without a rail were measuring it in the 240px rail column — fixed; and the list name's first cut ellipsized, which the probe's `-row` overflow check reads as overflow (321 in 203) — clamped instead, the row title's rule. **→ follow-up (P1.6):** the palette dots (`.tb-c-*`) are literal colours and the darker ones (moss, ink, wine) nearly vanish on a dark card. | v178 | run.js 5,936/5,936 · smoke-board 3/3 (124/129/129: the star puts the item in My Day without opening it) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 78/78 (new fragment: a list with the densest rows and Completed; proven by stopping the meta line wrapping — fails at 420) |
| P1.4 | `d38b96c` | **The detail pane (To Do pattern), replacing the drawer.** At ≥1440 the open item is the **frame's third column** beside the list (sticky, scrolling on its own), so the list stays on screen and clickable and the open row is marked; 1024–1439 the same, with the **rail folded to its 56px icons** so the list keeps its room; below 1024 it is still a panel over the page, and a full sheet on a phone. Top to bottom: kind · lock · close (Esc) → **the title card** (round check, the title — a **textarea sized to its content** so a two-line title shows whole, Enter commits, a pasted line break becomes a space — the star, the steps each with their own small check, "Add step" / "Next step") → **property rows** with icons: Add to My Day, Date (with "was …"), People, List, Lane, Priority, Kind (option labels Title Case) → the note ("Add a note") → Files, Comments, Activity as cards → footer: Hand over, Mark done / Reopen, delete (trash icon, owner only), "Created by you · Today". Every id and handler the drawer had is kept (`tb-d-*`, `tb-step-new`, `tb-handover`, `tb-filepick`), and it keeps the `tb-drawer` class the shortcut table and tests find it by. Section headers are Title Case (Files, Comments, Activity, Hand Over, Keyboard, Move To, Inbox). The list track is capped at 960 and the columns are packed left, so on a wide screen the pane sits beside the list rather than floating away from it (seen at 1920 on the screenshots). | v179 | run.js 5,955/5,955 · smoke-board 3/3 (129/134/134: at 1440 the detail is a sticky column, the list stays beside it, the open row is marked, the title shows whole, Escape closes and the frame returns to two columns) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 82/82 (new fragment: the whole frame with the pane open at 1900/1280; proven by breaking the pane's label ink — fails at 1.05:1) |
| P1.5 | `1b3ce85` | **The Dashboard: collapsible groups, and a right column.** The left column is ONE surface of **groups** — Overdue (count red), Due Today, My Day, Assigned to Me, Needs a Date, Next 7 Days — each header folding its group (chevron, `aria-expanded`), an empty group not drawn. The fold is per viewer in `localStorage` (`tb-dash-fold`), **cleaned on load** like the calendar prefs (decision 6): only the six known keys survive, anything unreadable, non-array or over 4 KB is deleted. The **right column** is exactly Assigned by Me, Deadlines, Inbox (the five newest, unread first), Team Today, Activity. **Decision: the "My Lists" chips (card 12) left the Dashboard** — the rail lists every list with its open count since P1.2, and the brief's right column names five cards without it (its CSS went too). The two columns are 1.55 : 1 and **stack whenever the content is narrow** — a container query on `.tb-main`, so opening the detail pane beside a 1280px list stacks them too, which a viewport query cannot know. **Found on the screenshots, fixed here:** Team Today turned the whole "3 open · 1 due today · 1 overdue" line red when one item was overdue — only the overdue count is red now (spec s10); and card rows (Team Today, Activity, the Inbox card) sat against the card's edge. | v180 | run.js 5,972/5,972 · smoke-board 3/3 (136/141/141: a group folds, the fold is remembered, it unfolds, no list chips) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 82/82 (proven by breaking the group header ink — fails at 1:1) |
| P1.6 | `e0c1493` | **The calendar, restyled.** A **hairline grid** (cells share 1px borders instead of gapped tiles), weekends tinted, out-of-month days dimmed but still drop targets, **today as an accent circle** on its number, a drop target ringed in the accent. **Month days show three pills at most — or two and "+N more"**, which opens that day in place ("Show less" folds it; stepping or changing the view closes it); the week shows every pill. **Markers are flag chips** on their day (plus the rail down its edge). The "+" is an icon that says which day it adds to. **The toolbar**: ‹ Today › and the range in Title Case ("October 2026", "12 Oct – 18 Oct"), Month | Week (+ By Person) and Me | Everyone as segmented controls, and **a row of avatars replacing the person dropdown** — **decision: a face shows that person across Everyone**, because Me + a person would show only the items you share; the same face again shows everyone. Any list / Any lane / Hide done / Clear. **The week by person** is the same hairline grid, today's column tinted, a 104px name column on tablets. **The P1.3 follow-up, done: the palette is variables with a dark set** — measured, the old dark shades were ink 1.03:1, moss 1.56, wine 1.59 on a dark card; the dark set is 5.6–9.6:1, and sand is darkened in light (2.37 → 3.77:1 on white). Pills and dots read the same variables. The tray says "Unscheduled". **Layout fragment split:** the week by person is never drawn on a phone, but the fragment rendered it at 420 anyway (the harness cannot know the viewport) — it now has its own fragment at 1900/1280/800. | v181 | run.js 5,995/5,995 · smoke-board 3/3 (149/154/154: a face shows that person across Everyone and taps back, four items on one day show two pills and "+2 more", which opens to show them all) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 88/88 (the new by-person fragment proven by breaking a name's ink) |
| P1.7 | `18e7ba4` | **flatpickr everywhere a date is picked, with the markers shown.** All four Board date fields — the pane's Date, the composer's date, the move-request date and the phone move sheet — stay native `<input type="date">` in the markup (their ids, values and `onchange` are what everything reads) and are **enhanced after each paint** with the vendored flatpickr: Monday first like the calendar, "Tue 20 Oct 2026" on screen, `Y-m-d` underneath, and **the drop's markers underlined on their days** with the label as a tooltip. flatpickr fires a real `change` on the original input, so no handler changed. **A disabled field (a lock you cannot move) stays native and disabled.** Robustness, each driven: every picker is destroyed before a repaint (or each repaint would leave a calendar on `<body>`); the composer's chip row re-enhances only its own field; a click inside an open calendar is not "outside" the composer; **an open picker holds live updates** (a repaint would tear it from under the pointer); and **Escape with a calendar open closes the calendar, not the pane**. On a phone flatpickr hands over to the native picker. Without flatpickr (an old cached shell, the node harness) the native field simply stays. | v182 | run.js 6,010/6,010 · smoke-board 3/3 (160/165/165: the pane's date is a picker, it opens, starts Monday, shows the markers, Escape keeps the pane, a picked day re-dates the item, no calendars left behind — the Escape guard and the enhancement each proven by removing them) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 88/88 |
| P1-ci | `ad594da` | **CI was red from `d38b96c` to `18e7ba4`, four pushes, while every local run passed, and I didn't check the check-runs before pushing.** Read from the job log, not guessed: `smoke-layout` failed 7 of 328, in two ways, both in the TEST rather than the app. (1) **The composer's chips were measured mid-transition**: CI saw the background at its start value while the text colour, which does not transition, was already final (for example 1.03:1 on a chip that reads fine at rest). Reproduced locally by forcing a transition in flight; the probe now **finishes running CSS transitions before measuring**. A chip whose ink is its own background still fails, checked. What started the transition on the runner could not be found: delaying the stylesheet 1.5 s locally did not do it. (2) **The drawer's Files "+ add" was reported as covered by BODY**: since P1.4 the drawer is a pane that scrolls inside its own box, and with the runner's fonts the button fell below that box. Reproduced by making the pane overflow; the fragment now lays the pane out at full height, the same override as the frame fragment. Separately, `302879a` had one "the probe never ran" failure, in one of its two jobs. It was not reproduced and not addressed. | v183 | run.js 6,010/6,010 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout 328/328 · each failure reproduced before the fix, and passing after it |
| merge | `4272a32` | **Merged `origin/main`** (PR #89, cutting masters on the fabric-issue gate pass, which shipped at `v167`). `sw.js` and three `index.html` `?v=` tags conflicted and were resolved past both. | v184 | run.js 6,038/6,038 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout 328/328 |
| R-live | `2feee31` | **Review fixes for P0.3 (live items), three confirmed majors.** (1) **Every item or list created locally showed twice.** Real Firestore hands a local write to the listener before `commit()` resolves, and the create path then pushed a second copy; nothing removed it until the next change. The three create paths now upsert by id and record the new doc in the listener's own map, so a rebuild from the maps can't drop it either. (2) **A snapshot from any listener cleared both read failures**, so a refused `board_items` read became an empty board the moment the lists listener delivered. Failures are now tracked per query: seeded from `loadTbData`'s first read, cleared only when that query delivers, and put back when a listener starts failing. (3) **Leaving a field by clicking a button lost the click.** A mouse press moves focus on mousedown; the flush repainted before mouseup and replaced the button. A press in progress now holds the repaint (bounded to 5 s, and cleared on window blur), and the flush waits for the click, or for a focusout with no press behind it (keyboard). Each piece was reverted in turn and fails a named assertion (push instead of upsert: 3; no map record: 1; clearing on any snapshot: 4; no seed handoff: 2; the old flush: 1; no bound: 1). | v185 | run.js 6,056/6,056 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 88/88 |
| R-composer | `74c4a28` | **Review fixes for P0.5 (the composer), three confirmed majors and the minors with them.** (1) **Double Enter created duplicates, and offline the composer never cleared.** It cleared only after `commit()` resolved, which takes a network round trip, and offline it waits until the connection returns. It now clears before the write. A refused write puts the text and chips back, unless something new has been typed since. (2) **Closing an empty composer swallowed the click that closed it.** Closing on pointerdown collapsed the ~120 px chip rows before the release, so the row under the pointer moved away. It now closes on click, whose target is already fixed. (3) **Typing a date into the field wiped it after the first digit**: the rebuild took the caret out, and 0002-10-05 read as "no date". The field is no longer rebuilt while it is being typed in (only its label updates). A value is taken only once it is a real day with a year between 2000 and 2099, since 0202 is valid on the way to 2026. Also: one `_tbQaHasContent` rule, so Escape and the outside click agree (Escape used to close with a picked lane still set); chips can switch off a lane or a person the title named; the caret goes back to the end after a repaint. Test gaps from the same review: every enabled chip must carry the caret guard, not just one; the "I own" half of Needs a date has its own fixture; smoke-board's "opens on focus" starts from a closed composer; and a chip click must cause exactly 0 full repaints. Each fix was reverted in turn and fails by name (11 in run.js; the chip-repaint revert fails smoke-board in all three modes). | v186 | run.js 6,086/6,086 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 88/88 |
| R-people | `29e7d59` | **Review fixes for P0.2 (people), from the review of `a96cddb`.** (1) **The calendar "+" dropped an @handle it could not assign, with no message.** An unresolved Board @handle was taken out of the title and silently lost, and a title that was only "@saim" was saved as the title. It now says so, in the same words as the quick-add, and refuses an empty title. (2) **A failed directory read showed every teammate as "not set up yet"** and pointed an owner at Sync accounts for four people who all have accounts. People now carry a `reason` (`missing` / `unread` / `ambiguous`), and Team Today, the pane, the chips, the preview and the toasts say which. The comment path also stops writing mention stats after a failed read, because the merge would reset each mentioned person's count to 1. (3) **`username` on a profile row is self-writable, and the first row claiming a handle won.** Your own handle now always resolves to your session. A handle two rows claim is flagged ("two profiles claim @afnan") and not assignable, rather than guessed. Conservative on purpose: a recreated account that left a stale row gets flagged rather than silently misassigned. (4) **The empty-state sentence showed above a Deadlines card full of gates.** It now ignores only Team Today. Each fix was reverted in turn and fails by name (7 reverts). | v187 | run.js 6,106/6,106 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 88/88 |
| P1.8 | `20522f7` | **Layout and contrast checks for every new Board screen.** Four new `smoke-layout` fragments, each filled with real text (the module hydrates with `textContent`, so an unfilled fragment measures empty boxes): Settings with a seed Preview and an error line; the flatpickr date picker open, restyled by `css/main.css` and linked in the order `index.html` uses, with a marker day; the calendar with one face chosen and a day opened past its cap; and the Dashboard beside the item pane at 1900/1280/1100. Each was broken on purpose and fails by name. **A real bug it found:** with the pane open at 1280px, Team Today crushed a teammate's name to **0px** (unshrinkable counts beside a `flex:1` name, the Profile-directory bug again). The name now keeps a floor and the counts wrap under it. **A probe bug it found:** the test server sent HTML with no charset, so every `—` and `→` in a fragment was measured as mojibake. It sends `charset=utf-8` now. Also: the last lower-case button labels (open the calendar, hand over, post, send, run seed, cancel/move, clear, close, the composer's today/tomorrow/next mon chips) now start with a capital, the P0.6 rule. | v188 | run.js 6,108/6,108 · smoke-board 166 ok · smoke-browser 8/8 · smoke-startapp 4/4 · layout 352/352 |
| merge | `01d3416` | **Merged `origin/main` at `a9fbad4`** (the warehouse handover: Umair marks a sale collected and Raees confirms it, plus its review fixes). Conflicts in `sw.js` (v188 here, v169 there) and `index.html` (the `main.css` tag). Resolved to **v189**, past both. Kept the flatpickr sheet ahead of `main.css`. **Noticed, not touched:** `main` now carries `firestore-debug.log` (2,465 lines of Firestore emulator output), committed with the warehouse work. It is not mine to delete, but it probably wants removing and adding to `.gitignore`. | v189 | run.js 6,204/6,204 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout 358/358 |
| R-picker | `9673ce4` | **Review fixes for P1.1/P1.7 (the date picker), from the review of `302879a`.** flatpickr's own sheet has rules MORE specific than our restyle, carrying literal colours, so three states leaked through. (1) **The year arrows were `rgba(0,0,0,.9)`**, invisible on the dark surface. (2) **A picked day shown outside its month** (the 31st in November's grid) kept flatpickr's blue at 60% opacity. (3) **A focused or hovered day from the next or previous month was a `#e6e6e6` slab**, light-on-light in dark mode, and a focused or hovered today was a grey `#959ea9` chip. All now take the tokens; the spinner's hover washes too. **No contrast bar catches these** (the blue is 2.7:1 under white), so `smoke-board` now puts a real picker in each state, in both themes, and requires that no computed colour on any element or its `::before`/`::after` is one of flatpickr's. Verified by reverting the CSS (8 fails), the outside-day rule alone (2) and the today rule alone (2). **Hover-only rules** (the month and year hover washes) cannot be driven and are held by nothing. **Also: the icon-name check read only literal first arguments,** so a name picked by a ternary, a rail `icon:'x'` or a pane `prop('x'` was never checked. The static check now reads all three (verified by breaking each), and `smoke-board` records every name `_tbIcon` is asked for at run time. | v190 | run.js 6,205/6,205 · smoke-board 3/3 (177 ok) · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 112/112 |
| R-seed | `e91c819` | **Review fixes for P0.4 (the seed), from the review of `9e3b521`. These were confirmed on the first review pass and had not been acted on; found while re-reading the review output.** (1) **Re-running the seed wiped people's work.** It merged every field but a short keep-list back onto each seeded item. That emptied `attachments`, put back renamed titles, kind, priority and list, made a privately-moved item `shared` again, and moved `ownerUid` to someone not on the item. Meanwhile the button, the confirm and the summary all promised the opposite. **A re-run now writes no existing item at all.** It keeps a record in `board_config/seed` (what it made, who each milestone was missing, who it put on the list), so: a milestone deleted since is **not brought back**; a person taken off the list is **not put back**; the list's own fields and existing markers are left alone; and the one addition, a person left off for want of a login who has one now, goes in by `arrayUnion`. A board seeded before the record existed is **adopted as it stands**, with nothing changed. (2) **Any Auth error was read as "no login"**, so a network blip could reshuffle people; now only `auth/user-not-found` counts, and anything else stops the run before it writes. (3) **The seed repainted the Board over whatever page the owner had gone to** while it ran; it repaints only on a Board page now. (4) **Tests:** the function's "it writes" asserted only `created:42`, which a dry run also returns, so a function that always dry-ran passed. It now asserts `dryRun:false` and 50 real writes. The Preview and Run seed buttons are now clicked through their own `onclick`, as are the confirm-cancel path and the re-reads after a real run. The keep-list loop that read the module's own list is replaced by "no `board_items` write at all". **Every fix was reverted and fails by name** (8 server, 4 client). `BOARD.md` "Running the seed" was stale (it still said dry-run by default and `--write`) and is rewritten; the CLAUDE.md line is corrected. | v191 | run.js 6,241/6,241 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 112/112 |
| R-pane | `8ca8275` | **Two pane bugs from the review of `d38b96c`. The verifiers never ran (session limit), so I checked both by reading the code, and both are real.** (1) **Notes could be saved to the wrong item.** The 800 ms autosave looked up whichever item was open when the timer fired. Typing in one item's notes and opening another within the window wrote the first item's text over the second's, and the first item's edit was lost. The pending text now carries the item it was typed in. Opening, closing or create-and-open flushes it to that item first, and the notes go into memory before the write, so reopening shows them (offline a write never resolves). A refused delete still keeps them. (2) **The thread arriving rebuilt the pane under someone typing.** Its callback repainted with no busy check. It now goes through the live repaint, which waits until the field is left. Reverted each: the old timer fails "nothing is ever written to the other item", no flush on close fails, no local apply fails, the direct repaint fails. **An extra delete-time "drop the pending notes" branch was removed**: its revert passed, because the id lookup already covers a deleted item, and it would have thrown away notes on a refused delete. **Test note:** a harness field that never loses focus keeps the deferred-repaint retry re-arming forever, so the process never exits. The new test releases focus as its positive control. | v192 | run.js 6,253/6,253 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 112/112 |
| R-listdefault | `aa2a3d4` | **From the review of `f256c83` (verifiers did not run; checked by reading, and real).** The open list (`_tbListId`) outlives the Lists page, because it is what the Lists page reopens, and every quick-add used it as the default list. So after opening any list, or pressing **+ New list** on the rail (which selected the new list behind whatever page you were on), a Dashboard or calendar add went **silently into that list and took its visibility**. With a shared list, a note-to-self became visible to the list's members. The default is now the list **on screen**: `_tbViewList()`, which applies only on the Lists page with that list open, is used at all seven composer sites. **+ New list** now opens the new list on the Lists page instead of selecting it out of sight. Reverted each: the old default fails three assertions (a Dashboard add lands in `L` and is `shared`, and the calendar defaults to it); repainting in place fails two. | v193 | run.js 6,260/6,260 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 112/112 |
| R-handlers | `9e47670` | **Stored script injection through inline handlers. From the review of `2ab0a8f` and `f256c83`; the verifiers did not run, so I checked it by reading the code and it is real.** The module puts ids and other values into 27 handlers of the form `onclick="window.f('…')"` after HTML escaping only. The browser decodes `&#39;` back to `'` **before** the handler runs, so a value carrying a quote closes the string, and whatever follows runs as script in the reader's session. Document ids are chosen by whoever writes the document. The inbox's `itemId` comes from `hrm_notifications`, which **anyone signed in** can write. New `_tbJs(v)` escapes for the JS string first (backslash, quote, line breaks, U+2028/9) and then for the attribute. Every site uses it. A test renders a row for four hostile ids (`x');box.pwned=true;//` and others), decodes the handler exactly as a browser would, runs it, and requires the id to arrive intact with nothing else run. The notification path is checked the same way. An invariant fails on any quoted handler argument that is not `_tbJs`. Reverting `_tbJs` to plain escaping **reproduces the exploit** (`box.pwned` set, 7 failures); putting one site back to `_tbEsc` fails the invariant by name. **Not checked:** other modules use the same pattern (for example `js/boards.js` and `js/marketing.js`). They are not this branch's to change, but they probably want the same review. | v194 | run.js 6,271/6,271 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 112/112 |
| R-tablet | `503ca12` | **From the review of `d38b96c` (two finders reported it; verifiers did not run). Measured, and real.** `.tb-wrap.has-pane` (three columns: rail, list, pane) sat outside any media query and outranked the tablet frame rule. At 640–1023px, opening an item reserved a 240px rail and an **empty 380px track** for a pane that is a fixed overlay at that width. Measured in headless Chromium at 800px: **the list column was 116px wide.** Scoped to `min-width:1024px`. Re-measured at 640 / 800 / 1023 / 1024 / 1100 / 1439 / 1440 / 1500: two columns with the overlay below 1024, and the 56px and 240px rails above it, unchanged. New `smoke-layout` fragment, *the Dashboard with the pane open, on a tablet* (800px, overlay hidden so the frame under it is measured). Against the old CSS it fails in both themes: the page title and every row title are laid out **0px wide**. | v195 | run.js 6,271/6,271 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · **full** layout 360/360 |
| R-noton | `f928b1f` | **From the review of `2ab0a8f`: "the My Day star appears on rows the viewer cannot write to". The verifiers did not run; I checked it against `firestore.rules`, and the gap was wider than the star.** The update rule lets only an assignee, the owner, the list's admin or a Board owner change an item. Every Board user can **read** a shared item, and the UI offered all of them the star, the tick, calendar drag, the move sheet and every pane field. Each was refused, with a toast blaming "a lock, or firestore.rules is not deployed yet". New pure `tbCanEdit` mirrors the rule's first clause. For someone not on the item: the row shows no star and a disabled tick that says why; drag and the move sheet refuse up front; `tbMovePlan` refuses with `notOn`; the **pane is read-only** (title and note `readonly`; list, lane, priority, kind, date and people disabled; no steps, files, Hand over or Mark done), with one line saying so. **Comments stay open**, since the comment rule allows any Board user. Every item write path refuses plainly before writing. **Behaviour change, deliberate:** someone who is not on a shared item can no longer try to change it. They comment instead, as the rules always required. Three tests encoded the old assumption ("an unlocked item is anyone's", with Daniyal not on it) or had a lock holder who wasn't on the item; they are corrected. `smoke-board`'s "an item this person can re-date" uses the same rule. Reverted each part: `tbCanEdit` returning true (17 failures), the row star (1), the pane (8), the star write guard (1), the move plan (3). | v196 | run.js 6,296/6,296 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 114/114 |
| R-launch | `1d429c7` | **Two launch findings from the review of `b43a3db` (verifiers did not run; checked by reading, both real).** All five Board users land on the Dashboard at every launch. (1) **The last-seen stamp raised the blocking "Saving…" overlay.** It wrote through the app's wrapped `setDoc`, which shows the overlay for any write slower than 260ms. On a phone at launch that is common; offline, the overlay held until its 25s failsafe. It now takes the documented `_gvSilentSaveStart/Stop` opt-out, **released after 2s even if the write never answers**, because an opt-out that outlived an offline write would silence every other module's overlay for the session. (2) **`loadData`'s closing `renderPage(currentPage)`** (in `js/pos.js`), which lands seconds after a Board user arrives, rebuilt the Board with no busy check, under a title being typed or between a press and its click. A re-render of the page **already on screen** now goes through the deferred live repaint. Navigating to a Board page from elsewhere is never held back. The fix is in `js/theboard.js`; `js/pos.js` is untouched. Reverted each: no opt-out (2 failures), no 2s cap (1), no deferral (2), deferring navigation too (1). **Noted, not fixed (shared, cross-track):** the write-buffer in `js/shared.js` counts a write that never resolves (offline) as in flight forever, so later writes in that session get no "Saving…" overlay at all. | v197 | run.js 6,304/6,304 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · layout Board 114/114 |
| R-rail | `2b4f6b3` | **From the review of `f256c83` (two finders; the verifiers did not run). Measured, and real.** On the ≥1440 rail, the lists are meant to scroll inside their group while the nav and foot stay put. But `.tb-railnav` had `min-height:0` and the default `flex-shrink:1`, so a long list squeezed it. At **1900×700 with 20 lists, Inbox was painted under the list labels.** The nav and foot are now `flex:0 0 auto`, and only the group gives up height. New fragment, *the rail with many lists, on a short screen*. **Also a probe fix, shared by both tracks:** `smoke-layout` reported a control scrolled out of view inside a scrollable ancestor as "covered by" the scroller. That was a documented false hit which several fragments had to work around, and it buried the real Inbox finding among 20 scrolled-away list rows. A control whose centre is outside a **scrollable** (`auto`/`scroll`) ancestor's visible box is now skipped. An `overflow:hidden` ancestor still counts, since nothing brings that control back. Verified: with the probe change and the old CSS, the fragment fails naming only Inbox; with the fix it passes; the full suite still passes 362/362. | v198 | run.js 6,304/6,304 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 · **full** layout 362/362 |
| R-landing | `505ee49` | **From the review of `b43a3db` (verifiers did not run; real by reading).** P0.7 lands the five Board users on the Board. If `js/theboard.js` failed to parse, `TB_HOME` was undefined, the code fell back to `'tb-dash'`, and **four people who have a normal home** (Ammar, Afnan and Mustafa: Dashboard; Daniyal: Creators) would have landed on the one line "The Board did not load". They now land on the Board only when its renderer exists (`typeof tbRenderPage==='function'`). Saim has no other page, and `showPage`'s designer scope still sends him to the Board. `js/auth.js` is shared; the change is confined to the landing lines this branch added. A test loads the app without `theboard.js`: Ammar lands on `dashboard`, Daniyal on `mkt-creators`. Reverting the guard fails both, with `tb-dash`. | v199 | run.js 6,307/6,307 · smoke-board 3/3 · smoke-browser 8/8 · smoke-startapp 4/4 |
| R-tests | *(this commit)* | **Two real test bugs from the review of `302879a` and `2b34547`.** (1) **A commented-out entry satisfied the wiring checks.** "Every `js/*.js` is in `PRECACHE_URLS`" and "has a `<script>` tag" matched the text anywhere in `sw.js` and `index.html`, comments included. They now read the array with its comments stripped, and `index.html` without `<!-- -->`, and the script check requires a real `<script src>`. Verified: commenting out `theboard.js` in either file fails by name. (2) **The case-transform invariant flagged `text-transform: none`** written with a space: backtracking let `\s*(?!none)` match the space before `none`. It is now `:(?!\s*none\b)`. Verified: `none` with a space passes, and `uppercase` still fails. **The other unverified findings from those reviews are left open**, all test-coverage gaps rather than defects: the Title Case checks look at first letters only; three of four `BUG_PAGE_NAMES` entries are unpinned; the sprite checks miss `stroke=`, `style=` and `xlink:href`; the stylesheet-order dependency is untested; the calendar week-count assertion cannot tell Mon–Sun from Sun–Sat; "the fold is per list" has one list in its fixture; the pane's property-row order and the 1024–1439 rail fold are not asserted. | v200 | run.js 6,307/6,307 · smoke-board 3/3 (unchanged code) |

---

## Rules / index deploys — batched

Nothing yet.

## → NEEDS YOU

- **R-people: if Team Today ever says "check profile" beside someone**, two
  `user_profiles` rows claim that username (most likely a stale row left
  by a recreated account, or someone writing their own row). An owner
  deletes the stale one; until then that person can't be assigned. Code
  can't tell the real row from the stale one, because `username` is
  self-writable under the current rules. Pinning it to the login email
  (`request.resource.data.username + '@groovy.op' == request.auth.token.email`
  on self-create/update) would close it at the source. That is a
  `user_profiles` rules change, and the Profile page belongs to both
  tracks, so it is **proposed, not made**.

- **P0.7 — tell Ammar's Marketing session** that `tests/marketing.test.js`'s
  "startApp lands the lead on it" now expects `showPage(boardHome||'mkt-creators')`:
  Daniyal lands on The Board; a lead who is not a Board user still lands on
  Creators.

- **P0.4 — once this is merged and deployed: Ammar or Afnan, open The
  Board → Settings → Preview, then Run seed.** Preview writes nothing and
  shows what it would do. Run seed writes the Winter Drop 2027 list, its
  42 milestones, the launch markers, and a profile row for each of the
  five (so Team today shows everyone). It is safe to press again.
  It uses the `FIREBASE_SERVICE_ACCOUNT` Netlify variable every other
  function already uses — nothing new to configure. If the button says
  "not on this site yet", the deploy has not finished.

- **P0.2 — press "Sync accounts" once** (Profile page → Team card, owners
  or Mustafa) so every Board person has a profile row. Until someone has a
  row they show on the Board as "not set up yet" and cannot be assigned.
  (P0.4's Run seed button also writes these rows.)
- **P0.1:** anyone who clicked Calendar before this ships has up to 1.36 MB
  of junk in their browser's storage. Nothing to do — the fixed loader
  deletes it on the first Calendar open.
